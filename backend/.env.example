# Flask Application Configuration
# Copy this file to .env and fill in your values

# Security Keys (Required)
# Generate strong random keys for production
SECRET_KEY=076862381ce53d0e8a8bdf752f2bdd5c2dab4d46f135cd097cefc5c4a581c1bb
JWT_SECRET_KEY=132ad78bc896d39994a7df962dfd402231c90d7e1ff18a90c1e4ec8509c61d7e

# Database Configuration
# SQLite for development, PostgreSQL/MySQL for production
DATABASE_URL=sqlite:///finance_tracker.db

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Logging Configuration
# Set to DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Rate Limiting
# Global rate limits (can be overridden per endpoint)
RATELIMIT_DEFAULT=200 per day,50 per hour

# JWT Configuration
# Token expiration in seconds (default: 1 hour)
JWT_ACCESS_TOKEN_EXPIRES=3600

# Database Connection Pool
# For production databases
SQLALCHEMY_POOL_SIZE=10
SQLALCHEMY_MAX_OVERFLOW=20
SQLALCHEMY_POOL_RECYCLE=300

